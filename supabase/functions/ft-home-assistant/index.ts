import{createClient}from'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,'Content-Type':'application/json'}});
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const base=(Deno.env.get('HOME_ASSISTANT_URL')||'').replace(/\/$/,'');const token=Deno.env.get('HOME_ASSISTANT_TOKEN')||'';const jwt=(req.headers.get('Authorization')||'').replace('Bearer ','');const{data:{user}}=await sb.auth.getUser(jwt);if(!user)return json({error:'Sign in required'},401);const{data:m}=await sb.from('ft_household_members').select('*').eq('user_id',user.id).limit(1).single();if(!m)return json({error:'Household required'},403);const body=await req.json().catch(()=>({}));const ha=async(path:string,init:RequestInit={})=>fetch(`${base}/api${path}`,{...init,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(init.headers||{})}});if(!base||!token)return json({error:'Home Assistant is not configured'},503);
 if(body.action==='available'){if(m.role!=='admin')return json({error:'Admin required'},403);const r=await ha('/states');if(!r.ok)return json({error:'Home Assistant connection failed'},502);const states=await r.json();return json({entities:states.filter((x:any)=>['light','switch','scene'].includes(x.entity_id.split('.')[0])).map((x:any)=>({entity_id:x.entity_id,domain:x.entity_id.split('.')[0],name:x.attributes.friendly_name||x.entity_id,state:x.state}))})}
 if(body.action==='save'){if(m.role!=='admin')return json({error:'Admin required'},403);const list=Array.isArray(body.entities)?body.entities:[];await sb.from('ft_home_entities').delete().eq('household_id',m.household_id);if(list.length)await sb.from('ft_home_entities').insert(list.map((x:any)=>({household_id:m.household_id,entity_id:x.entity_id,display_name:x.name,domain:x.domain})));return json({saved:true})}
 const{data:allowed}=await sb.from('ft_home_entities').select('*').eq('household_id',m.household_id).eq('enabled',true);if(body.action==='control'){const entity=allowed?.find((x:any)=>x.entity_id===body.entity_id);if(!entity)return json({error:'Device not allowed'},403);const service=entity.domain==='scene'?'turn_on':'toggle';const r=await ha(`/services/${entity.domain}/${service}`,{method:'POST',body:JSON.stringify({entity_id:entity.entity_id})});if(!r.ok)return json({error:'Control failed'},502)}const r=await ha('/states');if(!r.ok)return json({error:'Home Assistant connection failed'},502);const states=await r.json();return json({connected:true,is_admin:m.role==='admin',entities:(allowed||[]).map((e:any)=>{const s=states.find((x:any)=>x.entity_id===e.entity_id);return{...e,state:s?.state||'unavailable'}})})});
